name: blood-bank-server
service:
  name: web
  port: 3001
  env:
    - key: DATA_DIR
      value: /data
    - key: NODE_ENV
      value: production
  routes:
    - path: /
  health:
    path: /health
    interval: 30
    timeout: 5
  volumes:
    - name: data
      path: /data
      size: 1
